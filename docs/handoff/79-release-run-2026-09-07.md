# #79 release run handoff, 2026-09-07

Autonomous run on branch `t3code/1f0fa3c2` (PR #711) from `main` at
`4074a9a2`. Owner instruction: disposition every open issue as 1.0 or not,
implement what is needed, merge on green without waiting for the (unavailable)
cross-model review, record every decision, and leave a step-by-step release
plan if the code work turns out to be done. Decision log with the full
question/options/choice table:
`docs/reports/1.0/release-run-2026-09-07.html`.

## Outcome in one paragraph

No open issue needs code for 1.0. The acceptance ledger was already 42 of 49
INTERNAL-PASS with the rest being external evidence, the tag and the signing
ceremony. This run armed auto-merge on the one open bug-fix PR (#710), cleaned the
repository (31 MB of accidentally committed binaries, root-level private
handoffs), swept the 28 post-audit PRs for slop (one durability bug, three
duplicated helpers, fourteen dead functions), corrected two stale
status-ledger entries, closed two release-plan steps (goldens, enum audit),
and wrote `docs/release/release-plan-1.0.md`.

## What landed (PR #711)

| Commit | Summary |
| --- | --- |
| `chore(repo)` | `analysis-shards-go` and `nightly` binaries removed and ignored; `PRIVATE-*`/`private-*` files moved to `docs/handoff/security-2026-09/`; raw Vitest dump dropped. |
| `docs(status)` | `OBL-OPERATOR-PI-FIT` and `CAP-PRODUCTION-OPS` flipped to implemented against the native arm64 operator evidence; `release/trust/README.md` now describes the actual bootstrap state. |
| `refactor` | Dead code removed; `releaseidentity.SafeName` replaces three `safeName` copies; `securefile.WriteAtomic` syncs the directory after rename; `authn.PrivacyActivity` and the recovery grant readers deduplicated; 29 ` ,` comment artifacts fixed in `web/src`. |
| `test(web)` | Sensitivity inventory pins refreshed for six comment-only edits. |
| `docs(release)` | Release plan and decision log. |

## Verification

`go build ./...`, `go vet ./...`, `gofmt -l` clean, tests for every touched
Go package plus the privacy and recovery isolation tests, `pnpm --dir web
typecheck`, 948 web unit tests. CLI goldens regenerated with no diff.
Cross-model review skipped by owner instruction (Codex limit resets
2026-09-13, OpenCode without credits); the PR body says so.

## Human steps that remain (details in the release plan)

1. ~~Publish the six draft GHSAs.~~ Done 2026-09-08; CVEs requested.
2. Decide the signing-root custody (recommendation: fresh offline primary
   under the existing recovery root).
3. GitHub live provider run (seven fine-grained tokens, dedicated fixture).
4. MCP named-client proof against a public HTTPS deployment (#651).
5. Quarterly PVR receipt and public docs-endpoint check.
6. Tag `v1.0.0`, run the offline signing ceremony, publish.

## Not done, and why

- The #619 architecture leftovers, #683 Kubernetes condition reporting, #631
  MCP delegation and the social sign-in lane are post-1.0 by the owner's
  own dispositions; nothing in them blocks the tag.
- History was not rewritten to purge the 31 MB of binaries; that is a
  clone-wide, signed-chain-breaking decision for the owner.
- The audit-retention fallback and the thirteen sqlite/postgres dispatch
  pairs in `ErasePrivacyAccount` were left; both are recorded in the decision
  log.
